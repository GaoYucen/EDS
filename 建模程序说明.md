# 损坏模块建模分析程序说明

## 文件

- `damage_prediction.py`：主程序，无第三方依赖，优先解析 `data/` 目录内的两个 `.xlsx`，若不存在则回退到当前文件夹。
- `outputs/modeling_dataset.csv`：模块级建模宽表，每行一个模块。
- `outputs/chip_level_modeling_dataset.csv`：芯片级弱标签建模表，每行一个芯片位置。
- `outputs/risk_ranking.csv`：模块异常风险排名。
- `outputs/chip_level_risk_ranking.csv`：芯片级异常风险排名。
- `outputs/top_damage_indicators.csv`：损坏/正常模块差异最大的特征。若没有正常样本，该文件只有表头。
- `outputs/damage_prediction_summary.json`：本次运行摘要。
- `train_weak_chip_model.py`：在“芯片互不干扰”假设下训练芯片级弱标签 Logistic Regression 基线模型。
- `outputs/weak_chip_predictions.csv`：弱标签模型对当前芯片样本的分组交叉验证预测结果。
- `outputs/weak_chip_model.json`：使用全部当前芯片弱标签样本训练得到的临时模型参数。
- `outputs/weak_chip_model_metrics.json`：模型分组交叉验证指标和训练集拟合指标。
- `make_visualizations.py`：基于当前 CSV 输出生成 SVG 可视化图片，结果写入 `outputs/visualizations/`。

## 运行方法

在当前文件夹运行：

```powershell
python .\damage_prediction.py
python .\train_weak_chip_model.py
python .\make_visualizations.py
```

## 当前数据限制

当前两个 Excel 中共识别到 4 个模块，且 4 个模块都被芯片层级颜色标记命中为损坏模块，没有正常模块对照样本。

在暂时认为不同芯片互不干扰的前提下，程序额外导出芯片级弱标签数据：

- 标色芯片行：`label_damaged=1`，`label_role=marked_damaged`
- 未标色芯片行：`label_damaged=0`，`label_role=candidate_negative`
- `group_module_id` 保留原始模块 ID，用于后续按模块分组切分训练/验证集

注意：未标色芯片只是临时候选负样本，不等于经确认的真实正常样本。

当前弱标签预测模型也基于这个假设建立：

- 模型类型：标准库实现的 Logistic Regression。
- 输入特征：芯片原始数值参数、DBC 位置、芯片位置。
- 不使用 `process_anomaly_score` 等全数据派生异常分数作为训练特征，避免明显泄漏。
- 验证方式：按 `group_module_id` 留一模块交叉验证。
- 输出分数：`weak_model_probability`，只能解释为当前弱标签规则下的临时风险排序分数，不能解释为真实故障概率。

因此当前程序输出的是：

- 损坏标签整理
- 芯片层级聚合特征
- 芯片级弱标签样本
- 模块层级测试参数特征
- 基于参数离群程度的异常风险分数

这还不是可靠的监督式“损坏预测模型”。要训练分类模型，至少需要继续补充更多历史模块，尤其是正常模块和更多损坏模块。

## 后续可扩展方向

当样本增加后，可以在 `modeling_dataset.csv` 基础上继续训练：

- Logistic Regression
- Random Forest
- XGBoost / LightGBM
- Isolation Forest 异常检测

评估时重点看召回率、精确率、PR-AUC 和混淆矩阵，不建议只看准确率。
